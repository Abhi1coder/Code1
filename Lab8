Open terminal:
mkdir newpipeline
cd newpipeline
echo "<h1>CodePipeline MCA Lab</h1>">index.html
git init
git add .
git commit -m "first commit"

create git hub repo named Newpipeline
git remote add origin https://github.com/Abhi1coder/Newpipeline.git
If origin exists:
git remote set-url origin https://github.com/YOUR_USERNAME/Newpipeline.git

git branch -M main
git push -u origin main

-- optional -git config --global user.name "Your Name"  or git config --global --unset user.name
--optional -git config --global user.email "your_email@example.com" or git config --global --unset user.email

create bucket pipelinedemo

STEP 6 — Disable block public access
Permissions → Edit
Uncheck all 4 boxes.
Type:
confirm
Save.

STEP 7 — Add bucket policy
Paste exact policy:
{
"Version": "2012-10-17",
"Statement": [
{
"Effect": "Allow",
"Principal": "*",
"Action": "s3:GetObject",
"Resource": "arn:aws:s3:::bucket_name/*"
}
]
}

STEP 8 — Enable static website hosting
Properties → Static website hosting
Enable:
Index document = index.html
Save.

STEP 9 — Upload index.html manually first
Upload file:
index.html

STEP 10 — Verify website manually
Open website endpoint:
http://bucketname.s3-website-us-east-1.amazonaws.com

STEP 11 — Create AWS CodePipeline
Very important
Enable:
Extract file before deploy = ON

now change file manually
echo "<h1>Changed</h1>">index.html
git add .
git commit -m "version 2"
git push

refresh end point
